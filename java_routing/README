Code for computing walking times between stops.

Simple how-to
-------------

1. Install maven (https://maven.apache.org/), to have the mvn executable.
2. Compile with

	mvn package

3. Optional: for getting sources of dependencies for development

	mvn dependency:sources

4. Run with:
	
	java -jar target/transit_osm_routing-1.0-SNAPSHOT-jar-with-dependencies.jar -u ../examples/test_db_kuopio.sqlite -osm ../examples/data/kuopio_extract_mapzen_2017_03_15.osm.pbf --tempDir /tmp/

This projects uses JAVA8. If there are problems, make first sure you use java8 and not e.g java7.
(Find this out e.g. with command 'which java' and see where it points (recursively), or running 'java -version' )
(1.8 means Java 8)

